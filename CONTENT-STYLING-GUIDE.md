# 🎨 Руководство по Стилизации Контента

## Доступные цвета для стилизации

В вашем проекте используются следующие Tailwind CSS классы для цветов:

### 1. **Цвета текста**

```html
<!-- Основной цвет бренда (синий #0066cc) -->
<strong class="text-primary">Текст синим цветом</strong>

<!-- Основной цвет текста (темно-серый) -->
<strong class="text-base-content">Текст обычным цветом</strong>

<!-- Белый текст -->
<strong class="text-white">Белый текст</strong>

<!-- Полупрозрачный текст (70%) -->
<span class="text-base-content/70">Приглушенный текст</span>
```

---

## 📋 Примеры использования в Benefits

### Пример 1: Текст с акцентом на цифры
```yaml
checkpoints:
  - 'Od <strong class="text-primary">€2,600+ netto/miesiąc</strong> z przejrzystym systemem bonusowym ECO'
```

**Результат:** Od **€2,600+ netto/miesiąc** (синим) z przejrzystym systemem bonusowym ECO

---

### Пример 2: Смешанное выделение
```yaml
checkpoints:
  - 'Wypłata <strong class="text-base-content">zawsze na czas</strong> do <strong class="text-primary">10</strong> każdego miesiąca'
```

**Результат:** Wypłata **zawsze na czas** (жирный) do **10** (синий жирный) każdego miesiąca

---

### Пример 3: Только жирный текст (без цвета)
```yaml
checkpoints:
  - 'Flota <strong>nie starsza niż 5 lat</strong> – niezawodna'
```

**Результат:** Flota **nie starsza niż 5 lat** (жирный, без цвета) – niezawodna

---

## 🚗 Layout автомобилей

### Текущая настройка

```astro
<div class="grid grid-cols-1 md:grid-cols-2 xl:grid-cols-3 gap-8 lg:gap-12">
```

**Поведение:**
- **Мобильные (< 768px):** 1 колонка (все машины друг под другом)
- **Планшеты (768px - 1279px):** 2 колонки
- **Десктоп (≥ 1280px):** 3 колонки

### Как добавить 4-й автомобиль?

Создайте файл `/src/content/fleet/новый-автомобиль.md`:

```markdown
---
brand: "DAF"
model: "XF 480"
order: 4
image: "./../../assets/daf-xf.webp"
tag: "Efficient"
features:
  - "Super Space Cab"
  - "Predictive Cruise Control"
  - "EcoMode"
  - "Paccar MX-13"
---

Ваше описание автомобиля здесь...
```

**Результат:** 
- Десктоп: 2 ряда по 2 машины (4 в ряд не влезет в сетку из 3)
- Планшет: 2 ряда по 2 машины
- Мобильный: 4 машины друг под другом

---

## 🎯 Варианты Layout (если нужно изменить)

### Вариант 1: Всегда 2 колонки на больших экранах
```astro
<div class="grid grid-cols-1 lg:grid-cols-2 gap-8">
```

### Вариант 2: До 4 колонок на очень больших экранах
```astro
<div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 2xl:grid-cols-4 gap-8">
```

### Вариант 3: Flex с автоматическим переносом
```astro
<div class="flex flex-wrap gap-8">
  <!-- Каждая карточка займет ровно столько места, сколько нужно -->
</div>
```

---

## 📝 Полный список доступных Tailwind классов

### Цвета текста
- `text-primary` - Синий бренда (#0066cc)
- `text-base-content` - Основной текст
- `text-white` - Белый
- `text-neutral` - Нейтральный серый
- `text-error` - Красный (для ошибок)
- `text-success` - Зеленый (для успеха)
- `text-warning` - Желтый (для предупреждений)

### Прозрачность
- `text-primary/50` - 50% прозрачности
- `text-primary/70` - 70% прозрачности
- `text-primary/90` - 90% прозрачности

### Размеры текста
- `text-xs` - 12px
- `text-sm` - 14px
- `text-base` - 16px
- `text-lg` - 18px
- `text-xl` - 20px
- `text-2xl` - 24px
- `text-3xl` - 30px

### Вес шрифта
- `font-normal` - 400
- `font-medium` - 500
- `font-semibold` - 600
- `font-bold` - 700
- `font-extrabold` - 800
- `font-black` - 900

---

## 💡 Примеры комбинаций

### Большой синий жирный текст
```html
<span class="text-2xl font-bold text-primary">€3,000/месяц</span>
```

### Маленький приглушенный текст
```html
<span class="text-sm text-base-content/60">Дополнительная информация</span>
```

### Выделенная цифра в тексте
```html
Od <span class="text-3xl font-black text-primary">€2,600+</span> netto/miesiąc
```

---

## 🔧 Как применить в файлах Benefits

Откройте файл, например `src/content/benefits/salary-employment.md`:

```yaml
---
title: "Заголовок"
order: 1
icon: '...'
checkpoints:
  - 'Обычный текст с <strong class="text-primary">акцентом синим</strong>'
  - 'Текст с <span class="text-2xl text-primary">большой цифрой</span>'
  - '<strong class="text-base-content">Жирный</strong> и <em class="text-primary">наклонный синий</em>'
---

Описание преимущества (обычный markdown)
```

---

## ⚠️ Важно!

1. **Всегда используйте одинарные кавычки** в YAML для HTML:
   ```yaml
   ✅ Правильно: - 'Текст с <strong class="text-primary">акцентом</strong>'
   ❌ Неправильно: - "Текст с <strong class="text-primary">акцентом</strong>"
   ```

2. **Компонент уже настроен** для рендеринга HTML через `set:html`

3. **Не используйте markdown синтаксис** для стилизации (** не поддерживает цвета):
   ```yaml
   ❌ **€2,600** - только жирный, без цвета
   ✅ <strong class="text-primary">€2,600</strong> - жирный синий
   ```

---

## 📊 Текущая структура файлов

```
src/content/
├── faq/
│   ├── 1-schedule.md
│   ├── 2-trucks.md
│   └── ...
├── fleet/
│   ├── mercedes-actros-mp5.md
│   ├── scania-r450.md
│   └── volvo-fh.md (новый! ✨)
└── benefits/
    ├── salary-employment.md (обновлен! 🎨)
    ├── modern-fleet.md (обновлен! 🎨)
    └── friendly-team.md (обновлен! 🎨)
```

---

Если нужны дополнительные стили или цвета - дайте знать! 🚀
