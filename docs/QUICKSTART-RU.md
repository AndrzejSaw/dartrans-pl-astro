# Быстрый старт для разработчиков

## 📖 О документации

Этот проект содержит полную документацию для GitHub Copilot и разработчиков. Вся документация на английском языке, так как это международный проект.

## 📁 Структура документации

```
.github/
└── copilot-instructions.md    # Главная инструкция для GitHub Copilot

docs/
├── COMPONENTS.md               # Справочник по всем компонентам
├── API.md                      # Документация API endpoints
└── STYLING.md                  # Гид по стилизации

README.md                       # Основная документация проекта
```

## 🎯 Начало работы

### 1. Изучите основную документацию

**Обязательно к прочтению:**
- [.github/copilot-instructions.md](.github/copilot-instructions.md) - архитектура проекта, бизнес-контекст, правила разработки

**Для конкретных задач:**
- [docs/COMPONENTS.md](docs/COMPONENTS.md) - работа с компонентами
- [docs/API.md](docs/API.md) - работа с API
- [docs/STYLING.md](docs/STYLING.md) - работа со стилями

### 2. Работа с GitHub Copilot

GitHub Copilot автоматически использует всю документацию проекта для лучших подсказок.

**Примеры запросов к Copilot:**

```
"Как добавить новое поле в форму заявки?"
"Покажи валидацию для email в этом проекте"
"Создай новый компонент карточки следуя стилю проекта"
"Объясни как работает rate limiting"
"Добавь новый вопрос в FAQ компонент"
```

### 3. Основные паттерны проекта

#### Создание компонента
```astro
---
// src/components/NewComponent.astro
interface Props {
  title: string;
}
const { title } = Astro.props;
---

<section class="py-24 md:py-32 bg-neutral reveal">
  <div class="max-w-[85rem] mx-auto px-4 sm:px-6 lg:px-8">
    <h2 class="text-4xl font-heading font-extrabold">{title}</h2>
  </div>
</section>
```

#### Создание API endpoint
```typescript
// src/pages/api/endpoint.ts
import type { APIRoute } from 'astro';
import { z } from 'zod';

export const prerender = false;

const Schema = z.object({
  field: z.string()
});

export const POST: APIRoute = async ({ request }) => {
  const body = await request.json();
  const result = Schema.safeParse(body);
  
  if (!result.success) {
    return new Response(JSON.stringify({ error: 'Validation failed' }), {
      status: 400
    });
  }
  
  return new Response(JSON.stringify({ success: true }), {
    status: 200
  });
};
```

#### Стилизация компонента
```html
<!-- Кнопка -->
<button class="btn btn-primary btn-lg rounded-full px-12 hover:scale-105">
  Text
</button>

<!-- Карточка -->
<div class="card bg-base-100 shadow-xl hover:-translate-y-2 transition-all">
  <div class="card-body">
    <h3 class="card-title">Title</h3>
    <p>Content</p>
  </div>
</div>

<!-- Секция -->
<section class="py-24 md:py-32 bg-neutral reveal">
  <!-- Контент -->
</section>
```

## 🔧 Типичные задачи

### Добавление нового раздела на главную страницу

1. Создайте компонент: `src/components/NewSection.astro`
2. Используйте класс `reveal` для анимации появления
3. Следуйте дизайн-системе (цвета, шрифты, отступы)
4. Импортируйте в `src/pages/index.astro`
5. Добавьте в нужное место в разметке

### Изменение формы заявки

1. Откройте `src/components/LeadForm.astro`
2. Добавьте поле в HTML с Alpine.js биндингом
3. Обновите Zod схему в `src/pages/api/submit-form.ts`
4. Проверьте валидацию на клиенте и сервере

### Добавление нового стиля кнопки

1. Используйте существующие классы DaisyUI
2. Или создайте кастомный класс в `src/styles/global.css`
3. Следуйте паттернам из `docs/STYLING.md`

### Интеграция с новым API

1. Добавьте endpoint в `.env`
2. Создайте новый файл в `src/pages/api/`
3. Реализуйте rate limiting
4. Добавьте Zod валидацию
5. Документируйте в `docs/API.md`

## 📋 Чек-лист перед коммитом

- [ ] Код следует паттернам проекта
- [ ] Добавлены TypeScript типы (где нужно)
- [ ] Компоненты имеют правильную структуру классов
- [ ] API endpoints имеют валидацию Zod
- [ ] Responsive дизайн проверен (mobile, tablet, desktop)
- [ ] Accessibility: ARIA labels, semantic HTML
- [ ] GTM tracking добавлен (для интерактивных элементов)
- [ ] Нет console.log в production коде
- [ ] Проверена работа в dev режиме

## 🐛 Решение проблем

**GitHub Copilot не учитывает контекст проекта:**
- Убедитесь, что открыт файл `.github/copilot-instructions.md`
- Попробуйте переформулировать вопрос более конкретно
- Укажите файл документации в запросе: "Согласно COMPONENTS.md, как..."

**Нужна помощь с конкретным компонентом:**
- Откройте `docs/COMPONENTS.md`
- Найдите нужный компонент в содержании
- Изучите примеры и паттерны

**Ошибки валидации Zod:**
- Откройте `docs/API.md`
- Найдите схему валидации для вашего endpoint
- Проверьте формат данных

## 📞 Полезные ссылки

- **Astro Docs:** https://docs.astro.build
- **Tailwind CSS:** https://tailwindcss.com/docs
- **DaisyUI:** https://daisyui.com/components
- **Alpine.js:** https://alpinejs.dev
- **Zod:** https://zod.dev

## 💡 Советы по работе с проектом

1. **Всегда используйте TypeScript** для типизации props и данных
2. **Следуйте mobile-first подходу** при стилизации
3. **Используйте DaisyUI компоненты** вместо создания своих с нуля
4. **Добавляйте GTM tracking** для всех интерактивных элементов
5. **Проверяйте accessibility** всех новых компонентов
6. **Тестируйте API endpoints** с помощью curl или Postman
7. **Используйте rate limiting** для всех публичных API
8. **Документируйте сложную логику** комментариями

---

**Последнее обновление:** 4 февраля 2026  
**Версия проекта:** 0.0.1
